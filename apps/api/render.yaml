services:
  - type: web
    name: studentdeals-api
    env: node
    plan: free
    buildCommand: pnpm build
    startCommand: pnpm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
